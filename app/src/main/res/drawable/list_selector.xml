<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:exitFadeDuration="@android:integer/config_mediumAnimTime" >

    <!-- I never see this one - the grid items are not focusable -->
    <item android:state_pressed="false" android:state_focused="true" android:drawable="@drawable/main_sport" />

    <!-- while pressed -->
    <item android:state_pressed="true" android:drawable="@drawable/main_travel" />

    <!-- while selected in actionmode -->
    <item android:state_activated="true" android:drawable="@color/colorGray" />

    <item android:drawable="@android:color/transparent" />
</selector>